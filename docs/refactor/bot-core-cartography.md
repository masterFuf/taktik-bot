# [Bot] Cartographie `taktik/core`

## Scope

Snapshot realise le 2026-05-30 avant les premiers deplacements structurels.

Note de contexte :

- `AGENTS.md` n'existe pas a la racine de `c:\Users\kevin\Documents\taktik-desktop` ; la lecture preparatoire s'est appuyee sur `bot/AGENTS.md`, `front/AGENTS.md` et les docs de refactor demandees.
- Cette page sert de base vivante pour les lots commitables. Elle doit etre mise a jour avant tout deplacement important.

## Cartographie top-level

| Dossier | Owner cible | Role constate | Imports entrants principaux | Imports sortants principaux | Odeurs / risques | Proposition |
|---|---|---|---|---|---|---|
| `social_media/` | Plateformes | Workflows, actions, selectors et managers Instagram/TikTok/YouTube/Threads. 343 fichiers Python, zone dominante du core. | CLI, bridges, `agent`, `recorder`, tests. | `database`, `shared`, `clone`, `config`, `email`, `device` legacy. | Contient encore des sous-familles heterogenes et des events stdout historiquement proches des bridges. | Garder comme racine plateforme. Sortir progressivement DB/device/shared legacy par petits lots. Les events live Instagram passent maintenant par `IPCEmitter` avec adapter injecte par le bridge, sans import direct de bridge depuis `core`. |
| `shared/` | Shared technique Android | Primitives ADB/input/actions/platform partagees. | Principalement `social_media/**`. | Aucun import top-level sortant releve. | Frontiere globalement saine, mais `shared/utils/action_utils.py` doit rester tres limite pour ne pas redevenir un fourre-tout. | Canonical owner des primitives Android partagees. Pas de code metier plateforme ici. |
| `database/` | Persistence | Schema, migrations, client SQLite, repositories par domaine, modeles DB. | `social_media`, `agent`, `bridges/common` compat, tests unitaires. | Aucun import top-level sortant releve. | Ownership DB plus clair qu'avant, mais quelques facades legacy restent necessaires pour les workflows/bridges historiques. | Continuer la migration des acces DB vers repositories/facades `database/**`. Lot 2 deplace deja `processed / filtered / skip`, `processed_hashtag_posts` et le social graph legacy unfollow/sync vers des facades `database/instagram_*.py`, puis promeut ce dernier vers `repositories/instagram/social_graph/`. Le SQL `sent_dms` vit maintenant sous `repositories/messaging/` et `bridges/common/database.py` reste seulement une facade ; les modeles legacy vivent sous `models/instagram_profile.py`, pas sous un doublon `models/models.py`. |
| `device/` | Compat runtime | API legacy tres simple pour bridges/scripts/tests historiques. | Bridges debug/compat, scripts, quelques tests Instagram. | `shared/device/manager.py`. | Surface legacy encore consommee directement par quelques bridges/scripts, donc suppression immediate risquee. | Boundary de compat vers `shared/device/**`. L'implementation legacy statique vit sous `device/compat/legacy_static.py`; `device.py` reste seulement un shim d'import. |
| `clone/` | Runtime app Android clone-aware | Registry de package actif, proxy clone-aware, patch selectors, scan clones. | CLI, `recorder`, Instagram. | `compat`. | Zone proche de `compat`, donc risque de servir de depot passif si elle reste plate. | Garder comme couche d'adaptation package/app, pas comme dossier "divers". Structure interne actuelle : `detection/`, `packages/`, `device/`, `selectors/`; la racine reste une facade publique. |
| `compat/` | Compatibilite version/selectors | Routing de selectors par version, tracing compat et overrides YAML. | `clone`, bridges de compat, bridge Instagram desktop. | Aucun import top-level sortant releve. | Frontiere utile mais fragile si on y depose du debug ou du runtime generique. | Garder comme zone de compatibilite explicite, avec `compat/selectors/**` comme owner interne du framework selectors/versioning ; la racine reste seulement une facade `__init__.py`. |
| `agent/` | Runtime app / orchestration | Noyau d'orchestration transverse, contrats de plan, runtime parse/execute et scenario Instagram-first legacy. | Bridge agent, package local, tests unitaires. | `social_media`, `database`, `ai`. | Le scenario legacy depend encore directement d'Instagram et de SQLite, mais il est isole sous `scenarios/`. | Garder avec owner explicite "agent runtime". Structure interne actuelle : `kernel/` pour contrats/contexte/execution, `io/` pour JSON/manifest, `decision/` pour IA de decision, `scenarios/` pour autopilots legacy ; la racine du package reste seulement une facade `__init__.py`. |
| `ai/` | Runtime app / IA | Helpers IA du bot. | `agent`. | Aucun import top-level sortant releve. | Surface faible pour l'instant. | Garder isole ; eviter d'y melanger des effets Android ou DB. |
| `email/` | Integration runtime | Gmail workflow/selectors utilises par YouTube et certains flows de signup. | Bridges Gmail/YouTube, TikTok signup. | Aucun import top-level sortant releve. | Domaine transverse legitime, mais son persistence doit rester sous `database/repositories/gmail`. Le workflow Gmail dependait directement d'un IPC bridge. | Garder comme integration runtime ciblee, avec notifier/service injecte depuis l'appelant. Gmail est classe en `gmail/workflows/**` et `gmail/ui/**`. |
| `media/` | Integration technique | Facade runtime pour la capture media Instagram. L'implementation metier est maintenant sous `social_media/instagram/media/**`. | Bridge Instagram desktop, scripts setup/debug. | Aucun import top-level sortant releve. | Le package racine ne doit plus redevenir owner d'une integration media specifique a Instagram. Les assets proxy/Frida doivent etre resolus depuis `scripts/`, pas depuis la profondeur du package Python. | Garder comme facade package-level de compatibilite tant que `bridges/instagram/desktop_bridge.py` et les scripts importent encore `taktik.core.media`; ne pas recreer de sous-module shim profond si aucun consommateur interne ne l'utilise. |
| `social_media/instagram/recorder/` | Outil runtime / debug Instagram | Enregistreur de sessions humaines Instagram. | Scripts. | `clone`. | Ne doit pas remonter a la racine `core` tant qu'il est specifique a Instagram. | `taktik/core/recorder` a ete supprime ; importer l'owner direct Instagram. |
| `app/config/` | Runtime app | Endpoints et configuration runtime. | Quelques modules Instagram. | Aucun import top-level sortant releve. | Petit package, peu de code. | Owner final du lot config : pas de shim `taktik/core/config`. |
| `app/security/` | Runtime app | Protection / securite. | Tests hygiene. | Aucun import top-level sortant releve. | Surface actuellement faible ; risque de code mort ou de frontiere floue si on l'etend sans regle. | Owner final du lot security : pas de shim `taktik/core/security`, pas de `print(...)` dans ce package pour ne jamais risquer de polluer stdout bridge. |

## Hotspots a traiter par petits lots

| Priorite | Zone | Pourquoi c'est le bon lot | Direction |
|---|---|---|---|
| 1 | `device/` vs `shared/device/` | Duplication immediate, surface petite, nombreux call sites legacy. | Transformer `device/` en boundary de compat ; `shared/device/**` devient l'owner implementation. |
| 2 | `social_media/instagram/actions/business/common/database_helpers.py` | Decision DB encore dans une plateforme, contraire a la taxonomie cible. | Extraction progressive vers `database/**` sans casser les workflows. Sous-lots deja faits : `processed / filtered / skip` via `database/instagram_workflow_state.py`, tracking `processed_hashtag_posts` via `database/instagram_hashtag_posts.py`, puis bloc `unfollow sync` via `database/instagram_follow_graph.py`. |
| 3 | `social_media/*/core/manager.py` et `shared/platform/social_media_base.py` | Plusieurs managers importent encore des chemins Instagram legacy pour des concerns generiques. | Revenir a une base shared claire pour les managers applicatifs. |
| 4 | `clone/` vs `compat/` | Les deux zones sont legitimes mais proches conceptuellement. | Documenter leur frontiere : package/app runtime d'un cote, version/selectors de l'autre. |
| 5 | `recorder` et `media` | Zones techniques transverses, mais potentiellement trop couplees a Instagram desktop. | `recorder` est tranche : owner Instagram direct. `media` reste facade runtime tant que les imports bridge/scripts existent. |

## Regles deja deduites de la cartographie

- `shared/device/**` est la source de verite pour la connexion device, l'ATX et les primitives Android partagees.
- `device/` peut survivre comme compat import boundary, mais ne doit plus recevoir de nouvelle implementation propre.
- Une facade plateforme peut vivre sous `social_media/<platform>/.../device/` si elle ajoute un sens metier plateforme ; un `DeviceManager` a cet endroit doit etre un shim ou une specialisation explicite, pas une troisieme implementation generique.
- Une aide DB historique reste acceptable temporairement dans une plateforme seulement comme facade de compat documentee ; toute nouvelle logique SQL doit vivre sous `database/**`.

## Lots proposes

1. Lot 1 - boundary `device/` : doc + shim compat vers `shared/device/**` + tests unitaires.
2. Lot 2 - ownership DB Instagram + messaging : sous-lots faits pour `processed / filtered / skip`, `processed_hashtag_posts` et `unfollow sync`. Le SQL `following_sync` / `followers_sync` vit maintenant dans `repositories/instagram/social_graph/`, la famille `unfollow` appelle deja `InstagramFollowGraphService` directement, la famille `hashtag` appelle deja `InstagramHashtagPostService` directement, le runtime Instagram (business + UI extractors) appelle maintenant `InstagramWorkflowStateService` directement pour `processed / filtered / skip`, `workflows/post_scraping/post_persistence.py` ne porte plus de SQL inline sur `instagram_profiles`, et le scraping Instagram partage maintenant une dependency `local_db` explicite entre `scraping_workflow`, `persistence`, `list_scraping` et `deep_qualify` au lieu de multiplier les `get_local_database()` caches dans chaque mixin. `sent_dms` est maintenant porte par `repositories/messaging/SentDMRepository`, avec `database/messaging.py` comme facade service et `bridges/common/database.py` comme compat. `DatabaseHelpers` ne sert plus qu'en shim de compatibilite package-level. Prochain pas naturel : auditer si cette API de compat est encore necessaire hors repo avant suppression ou re-export plus fin.
3. Lot 3 - managers runtime : `instagram/core/manager.py` et `tiktok/core/manager.py` pointent maintenant vers `shared/platform/social_media_base.py` et `shared/device/manager.py` comme `threads/core/manager.py`, au lieu de repasser par les shims Instagram. Les workflows Instagram `scraping`, `post_scraping` et `cold_dm` consomment aussi directement `shared/device/manager.py` quand ils n'ajoutent aucun comportement plateforme, le CLI (`taktik/cli/main.py`, `commands/management_cmds.py`) utilise maintenant le meme owner shared, les scripts de test/debug Instagram restants convergent aussi vers `shared/device/manager.py`, et `taktik/core/__init__.py` expose de nouveau une compat top-level coherente pour `DeviceFacade`, `Direction` et `DeviceManager` via lazy exports valides. Prochain pas naturel : auditer les quelques re-exports de compat encore utiles (`actions/core/device`, `taktik/core/device`) et decider s'ils restent surfaces publiques volontaires ou s'ils peuvent etre deprecies davantage.
4. Lot 4 - clarifier `clone/compat` : `clone/**` reste l'owner du runtime clone-aware (package actif, proxy device, patch package-name), tandis que `compat/**` reste l'owner de la compatibilite selectors/versioning. Le framework selectors/versioning de `compat` vit maintenant sous `compat/selectors/**`, les bridges internes pointent vers ces owners directs, et les anciens modules top-level ont ete retires. `clone/packages/package_map.py` devient la source de verite pour les package names officiels et prefixes de clone, puis `clone/**` est splitte par owner (`detection`, `packages`, `device`, `selectors`).
5. Lot 5 - auditer `recorder/media/agent/email/ai/config/security` selon l'usage reel. Sous-lots faits le 2026-05-31 : l'implementation du human recorder Instagram vit maintenant sous `social_media/instagram/recorder/human_behavior_recorder.py` et la facade racine `taktik/core/recorder` a ete supprimee ; `config/security` ont ete migres sans shims vers `app/config` et `app/security` ; l'implementation de capture media Instagram vit maintenant sous `social_media/instagram/media/**`, tandis que `taktik/core/media` reste une facade package-level de compatibilite et que la resolution des assets `mitm_addon.py` / `frida_ssl_bypass.js` se fait depuis `scripts/` ; `core/email/gmail/gmail_workflow.py` ne depend plus directement de `bridges.common.ipc` et recoit maintenant un notifier optionnel injecte par les bridges/workflows appelants.
