"""Selectors of windows that belong to no platform.

A platform's screens are catalogued under `social_media/<platform>/ui/selectors/**`. A window
drawn by Android itself, over whichever app asked, is catalogued here, by perimeter:
`system/**` for the operating system's windows. Imports no platform module.
"""
